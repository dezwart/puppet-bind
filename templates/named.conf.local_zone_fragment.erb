
// Zone fragment for <%= name %>

zone "<%= name %>" IN {
    type <%= mode %>;
    file "/etc/bind/db.<%= name %>";
    <% if mode == 'slave' -%>
      master { transfer-servers; };
      allow-transfer { transfer-servers; };
    <% end -%>
};
