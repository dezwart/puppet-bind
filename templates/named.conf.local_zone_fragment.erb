
// Zone fragment for <%= name %>

zone "<%= name %>" IN {
    type <%= mode %>;
    file "/var/lib/bind/db.<%= name %>";
    <% if mode == 'slave' -%>
      masters { <%= masters %>; };
    <% end -%>
};
