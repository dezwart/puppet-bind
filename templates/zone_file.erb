$TTL    <%= ttl %>
@   IN  SOA <%= mname %>. <%= rname %>.<%= mname %>. (
    <%= serial %>; Serial
    <%= refresh %>; Refresh
    <%= failed_refresh_retry %>; Retry
    <%= expire %>; Expire
    <%= minimum %>; Minimum TTL
);

<% if @records %><%records.each do |record| %>
<%= record %><% end %><% end %>
